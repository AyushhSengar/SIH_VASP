# The API's origin. The frontend and the API are separate applications; this is
# the only thing the frontend needs to know about the backend.
#
# Copy this file to `.env.local` to override it. `.env.local` is gitignored, so
# a local override never travels.
VITE_API_BASE_URL=http://127.0.0.1:8000
