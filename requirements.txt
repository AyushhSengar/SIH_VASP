httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
pytest==8.3.3
pytest-asyncio==0.24.0
tenacity==9.0.0
networkx==3.4.2
scikit-learn==1.5.2
numpy==2.1.1

# --- Macro Milestone 6: persistence + investigation service + FastAPI ---
sqlalchemy==2.0.35
fastapi==0.115.0
uvicorn==0.30.6
# HTTP client FastAPI's TestClient needs (installed transitively by
# starlette/fastapi in most environments, pinned explicitly here so
# `pytest tests/` never depends on that being implicit).
httpx==0.27.2

# PostgreSQL driver, added separately per the M6 spec ("add PostgreSQL
# driver separately if appropriate") — SQLite is the default and no
# PostgreSQL server needs to be running for `DATABASE_URL` to stay
# sqlite:// during local dev/tests. Only needed if DATABASE_URL points
# at a postgres:// URL.
# psycopg2-binary==2.9.9